import { StatusColorDirective } from './status-color.directive';

describe('StatusColorDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new StatusColorDirective();
    expect(directive).toBeTruthy();
  });
});
